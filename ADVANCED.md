# Advanced

## Custom rules

Define `customValidationRules()` in your request/controller class that uses the trait.

## Events

- `auto-validator.validating`
- `auto-validator.validated`
- `auto-validator.failed`

## Strict mode

Enable strict mode in config to fail unknown fields (future extension point).
